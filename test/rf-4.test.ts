import { describe, it, expect } from "vitest";

describe("Gestión de reservas", () => {
	it("Administrador crea reservas", () => { expect(42).toBe(42); });
	it("Resepcionista crea reservas", () => { expect(42).toBe(42); });
	it("Huesped crea reservas", () => { expect(42).toBe(42); });
	it("Administrador cansela reservas", () => { expect(42).toBe(42); });
	it("Resepcionista cansela reservas", () => { expect(42).toBe(42); });
	it("Huesped cansela reservas", () => { expect(42).toBe(42); });
	it("Administrador edita reservas", () => { expect(42).toBe(42); });
	it("Resepcionista edita reservas", () => { expect(42).toBe(42); });
});
