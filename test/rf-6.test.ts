import { describe, it, expect } from "vitest";

describe("Facturación", () => {
	it("El administrador genera factura", () => { expect(42).toBe(42); });
	it("El huesped genera factura", () => { expect(42).toBe(42); });
	it("El recepcionista genera factura", () => { expect(42).toBe(42); });
});
